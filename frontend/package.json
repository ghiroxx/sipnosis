{
  "name": "sipnosis-static-frontend",
  "version": "1.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && [ -d bridge ] && cp -R bridge dist/bridge || true",
    "start": "python3 -m http.server 3000"
  }
}
